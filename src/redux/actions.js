// import { createAction } from '@reduxjs/toolkit';
// import { nanoid } from 'nanoid';

// export const addContact = createAction('contact/addContact', (name, number) => {
//   return {
//     payload: {
//       id: nanoid(),
//       name,
//       number,
//     },
//   };
// });

// export const deleteContact = createAction('contactList/deleteContact');

// export const filter = createAction('contactFiter/filter');
